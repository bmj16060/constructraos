micronaut:
  server:
    port: 0

datasources:
  default:
    db-type: h2
    dialect: H2
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:constructraos-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""

flyway:
  datasources:
    default:
      enabled: false

policy:
  service-enforce: false
